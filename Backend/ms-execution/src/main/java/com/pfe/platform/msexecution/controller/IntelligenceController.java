package com.pfe.platform.msexecution.controller;

import org.springframework.web.bind.annotation.RequestMapping;
import org.springframework.web.bind.annotation.RestController;

/**
 * IntelligenceController: core intelligence endpoints.
 * Note: UX-specific endpoints have been moved to UxEvaluationController
 * and no longer use ExecutionResult/TestCase models.
 */
@RestController
@RequestMapping("/api/intelligence")
public class IntelligenceController {

}
